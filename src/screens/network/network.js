import { View, Text } from "react-native";

export const Network = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "#0B0B0B",
      }}
    >
      <Text style={{ color: "#fff" }}>Network Coming Soon</Text>
    </View>
  );
};
